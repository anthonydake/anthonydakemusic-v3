"use client";

export default function ProjectsPlaceholder() {
  return (
    <section className="relative flex min-h-screen items-center justify-center bg-white text-black">
      <div className="text-center">
        <div className="text-[12px] uppercase tracking-[0.3em] text-black/60">Projects</div>
        <div className="mt-4 text-[clamp(18px,3vw,36px)] uppercase tracking-[0.08em] text-black">
          Project Index Placeholder
        </div>
        <div className="mt-3 text-[12px] tracking-[0.1em] text-black/50">Content loading soon.</div>
      </div>
    </section>
  );
}
